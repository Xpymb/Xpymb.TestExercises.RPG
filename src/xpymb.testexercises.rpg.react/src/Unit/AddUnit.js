import React from "react";
import PropTypes from "prop-types";

function AddUnit({ apiUrl }) {
    return <div>AddUnit</div>
}

AddUnit.PropTypes = {
    apiUrl: PropTypes.string.isRequired
}

export default AddUnit